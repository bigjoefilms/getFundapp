import React from "react";

interface CampaignCardProps {
  title: string;
  description: string;
  raised: string;
  daysLeft: number;
  owner: string;
  color: string; // Background color for the top section
}

const CampaignCard: React.FC<CampaignCardProps> = ({
 
}) => {
  return (
    <div className="">
      
    </div>
  );
};

export default CampaignCard;
